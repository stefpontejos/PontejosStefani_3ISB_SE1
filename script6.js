class Dog{
    constructor(name, breed, color){
        this.name=name;
        this.breed=breed;
        this.color=color;
    }
    displayInfo(){
        console.log(Dog.name + Dog.breed + Dog.color);
    }
}

Dog.prototype.eat(food)= fucntion(){
    if(Dog.name == bantay || DOMQuad.food==fish){
        console.log("Bantay likes fish");
    }
}
